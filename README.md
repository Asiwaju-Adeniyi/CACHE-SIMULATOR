# CACHE-SIMULATOR
C++ CPU cache simulator modeling a 32-bit system with 4MB main memory and a 16KB 4-way set-associative cache (64 sets). Uses a write-through policy and random replacement. Includes read/write tests and basic cache thrashing. Foundation for future improvements.
